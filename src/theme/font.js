import {
  Permanent_Marker,
} from "next/font/google";

// define your variable fonts
const permanent_marker = Permanent_Marker({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});


export {
  permanent_marker,
};

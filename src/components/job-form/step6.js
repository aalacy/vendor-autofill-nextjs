import { Grid } from "@mui/material";

import { InputField, SelectField } from "src/components/widgets";

export const Step6 = (props) => {
  const {
    formField: { shipAccount, shipAccountNumber },
  } = props;

  return (
    <>
      <Grid container spacing={3}></Grid>
    </>
  );
};
